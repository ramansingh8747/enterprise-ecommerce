import React from 'react';

/**
 * Unauthorized Page Placeholder (Module 7 - Step 7.4).
 *
 * Displayed when a user attempts to access a resource they are not permitted to view.
 */
const UnauthorizedPage: React.FC = () => {
  return <div data-testid="unauthorized-page" />;
};

UnauthorizedPage.displayName = 'UnauthorizedPage';

export default UnauthorizedPage;
