import type { ThemeOptions } from '@mui/material/styles';

/**
 * Enterprise Material UI Shape Configuration (Module 3 - Step 3.2).
 *
 * Configures global numeric border radius (8px matching BORDER_TOKENS.radius.lg).
 */
export const shapeOptions: NonNullable<ThemeOptions['shape']> = {
  borderRadius: 8,
};
