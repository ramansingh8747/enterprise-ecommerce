/**
 * UI Layout Constants (Module 2 - Step 2.2).
 */

export const UI_LAYOUT = Object.freeze({
  DRAWER_WIDTH: 260,
  DRAWER_COLLAPSED_WIDTH: 72,
  HEADER_HEIGHT: 64,
  FOOTER_HEIGHT: 56,
  MAX_CONTAINER_WIDTH: 1280,
});

export const UI_TIMINGS = Object.freeze({
  DEBOUNCE_MS: 300,
  THROTTLE_MS: 500,
  SNACKBAR_AUTO_HIDE_MS: 5000,
  ANIMATION_SPEED_MS: 200,
});
