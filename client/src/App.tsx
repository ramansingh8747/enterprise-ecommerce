import React from 'react';
import { AppRouter } from '@/routes';

/**
 * Enterprise Application Root Component (Module 4 - Step 4.2).
 *
 * Renders the master AppRouter entry point.
 */
export const App: React.FC = () => {
  return <AppRouter />;
};

export default App;
