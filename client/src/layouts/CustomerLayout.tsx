import React from 'react';
import type { ILayoutProps } from './layout.types';

/**
 * Enterprise Customer Portal Layout (Module 4 - Step 4.4).
 *
 * Layout placeholder for logged-in customer account pages.
 */
export const CustomerLayout: React.FC<ILayoutProps> = ({ children }) => {
  return <>{children}</>;
};

export default CustomerLayout;
