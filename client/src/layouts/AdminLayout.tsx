import React from 'react';
import type { ILayoutProps } from './layout.types';

/**
 * Enterprise Admin Dashboard Layout (Module 4 - Step 4.4).
 *
 * Layout placeholder for administrator management dashboard pages.
 */
export const AdminLayout: React.FC<ILayoutProps> = ({ children }) => {
  return <>{children}</>;
};

export default AdminLayout;
