import React from 'react';
import type { ILayoutProps } from './layout.types';

/**
 * Enterprise Authentication Layout (Module 4 - Step 4.4).
 *
 * Layout placeholder for login, register, and password reset pages.
 */
export const AuthLayout: React.FC<ILayoutProps> = ({ children }) => {
  return <>{children}</>;
};

export default AuthLayout;
