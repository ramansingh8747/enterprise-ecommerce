import React from 'react';
import type { ILayoutProps } from './layout.types';

/**
 * Enterprise Public Storefront Layout (Module 4 - Step 4.4).
 *
 * Layout placeholder for public storefront pages.
 */
export const PublicLayout: React.FC<ILayoutProps> = ({ children }) => {
  return <>{children}</>;
};

export default PublicLayout;
